class Ingredient {
  String name, quantity, imageUrl;

  Ingredient({
    String name,
    quantity,
    imageUrl,
  }) {
    this.name = name;
    this.quantity = quantity;
    this.imageUrl = imageUrl;
  }
}
